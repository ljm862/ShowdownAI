const { clickButton } = require("./clickButton");
const { getPageByUrl } = require("./getPageByUrl");
const { inputText } = require("./inputText");

module.exports = {
  clickButton,
  getPageByUrl,
  inputText,
};
